apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: myapp
  template:
    metadata:
      labels:
        app: myapp
    spec:
      containers:
        - name: myapp
          image: salieri20/to-do:e11da11e28a7dbadfeec6ef066fc6f98a27aebb4
          ports:
            - containerPort: 3000
          env:
            - name: mongoDbUrl
              value: mongodb://mongodb:27017/mydb
              
---
apiVersion: v1
kind: Service
metadata:
  name: myapp
spec:
  type: NodePort
  selector:
    app: myapp
  ports:
    - port: 4000
      targetPort: 4000
      nodePort: 32001
